<project name="installer" default="all" basedir=".">
	<description>caGrid Installer build file</description>
	<!-- set global properties for this build -->
	<property name="src" location="src" />
	<property name="build" location="build" />
	<property name="build.classes" location="${build}/classes" />
	<property name="build.jars" location="${build}/jars" />
	<property name="lib.dir" location="lib" />
	<property name="ext.lib.dir" location="ext/lib" />
	<property name="javadoc.dir" value="docs/api" />
	<property environment="env" />

	<property file="${basedir}/ext/resources/cagrid.properties" />
	<property name="project.jar.prefix"
		value="${cagrid.master.jar.prefix}${ant.project.name}${cagrid.master.jar.separator}" />

	<path id="build.classpath">
		<fileset dir="${lib.dir}" includes="**/*.jar" />
		<fileset dir="${ext.lib.dir}" includes="**/*.jar" />
	</path>
	<path id="run.classpath">
		<path refid="build.classpath"/>
		<pathelement location="${build.classes}"/>
		
	</path>

	<target name="init" depends="clean">
		<tstamp />
		<mkdir dir="${build}" />
		<mkdir dir="${build.classes}" />
		<mkdir dir="${build.jars}" />
	</target>

	<target name="compile" depends="init"
		description="compile the source ">
		<!-- Compile the java code from ${src} into ${build} -->
		<javac destdir="${build.classes}" source="1.5" debug="true">
			<src path="${src}" />
			<classpath refid="build.classpath" />
		</javac>
		<copy todir="${build.classes}">
			<fileset dir="${src}">
				<exclude name="**/*.java" />
				<exclude name="**/*.html" />
				<exclude name="**/*.psd" />
			</fileset>
		</copy>
	</target>

	<target name="clean" description="clean up">
		<delete dir="${build}" />
	</target>

	<target name="jar-core" depends="compile">
		<jar destfile="${build.jars}/${project.jar.prefix}core.jar"
			basedir="${build.classes}"
			includes="org/cagrid/**/*.class images/** test/** **/*.properties"
			manifest="manifest.txt"/>
	</target>

	<target name="jar" depends="jar-core"></target>

	<target name="all" depends="clean,compile,jar"></target>

	<target name="run" depends="all">
		<java classname="org.cagrid.installer.WizardLauncher"
			fork="true">
			<classpath refid="run.classpath"/>

		</java>
	</target>

	<target name="javadoc">
		<mkdir dir="${javadoc.dir}" />
		<javadoc destdir="${javadoc.dir}" author="true" version="true"
			use="true" windowtitle="CaGrid Installer API" classpathref="run.classpath">

			<fileset dir="src" defaultexcludes="yes">
				<include name="**/*.java" />
			</fileset>

			<doctitle>
				<![CDATA[<h1>caGrid Installer API</h1>]]>
			</doctitle>
			<bottom>
				<![CDATA[<i>Copyright &#169; 2000 NCICB. All Rights Reserved.</i>]]>
			</bottom>
		</javadoc>
	</target>
	
	<target name="dist-cagrid">
		<!--
		<property name="cagrid.home" value="/Users/joshua/packages/cagrid/1.1/cagrid-1-0"/>
		-->
		<mkdir dir="build"/>
		<property name="cagrid.home" value="../../.."/>
		<zip destfile="build/caGrid.zip">
			<fileset dir="${cagrid.home}">
				<include name="caGrid/**"/>
				<exclude name="caGrid/projects/portal*"/>
				<exclude name="caGrid/projects/installer/installers/**"/>
				<exclude name="caGrid/projects/installer/deployer/temp/**"/>
				<exclude name="caGrid/projects/installer/deployer/software/**"/>
				<exclude name="caGrid/projects/installer/deployer/certs/**"/>
				<exclude name="**/bin/**"/>
			</fileset>
		</zip>
	</target>
	
	<target name="dist-installer" depends="jar">
		<mkdir dir="build/installer/lib"/>
		<copy todir="build/installer" file="${build.jars}/${project.jar.prefix}core.jar" overwrite="true"/>
		<copy todir="build/installer/lib">
			<fileset dir="../dorian/lib">
				<include name="mysql-connector*.jar"/>
			</fileset>
			<fileset dir="lib">
				<include name="commons-logging*.jar"/>
				<include name="log4j*.jar"/>
			</fileset>
			<fileset dir="ext/lib">
				<include name="caGrid*wizard.jar"/>
			</fileset>
		</copy>
		<zip destfile="build/installer.zip" basedir="build/installer"/>
	</target>

</project>


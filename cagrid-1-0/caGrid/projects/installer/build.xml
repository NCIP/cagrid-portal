<project name="installer" default="all" basedir=".">
	<description>
        caGrid Installer build file
    </description>
	<!-- set global properties for this build -->
	<property name="src" location="src" />
	<property name="build" location="build" />
	<property name="build.classes" location="${build}/classes" />
	<property name="build.jars" location="${build}/jars" />
	<property name="lib.dir" location="lib" />
	<property environment="env" />

	<property file="${basedir}/ext/resources/cagrid.properties" />
	<property name="project.jar.prefix" value="${cagrid.master.jar.prefix}${ant.project.name}${cagrid.master.jar.separator}" />

	<path id="build.classpath">
		<fileset dir="${lib.dir}" includes="**/*.jar" />
	</path>

	<target name="init" depends="clean">
		<tstamp />
		<mkdir dir="${build}" />
		<mkdir dir="${build.classes}" />
		<mkdir dir="${build.jars}" />
	</target>

	<target name="compile" depends="init" description="compile the source ">
		<!-- Compile the java code from ${src} into ${build} -->
		<javac destdir="${build.classes}" source="1.4">
			<src path="${src}" />
			<classpath refid="build.classpath" />
		</javac>
		<copy todir="${build.classes}">
			<fileset dir="${src}">
				<exclude name="**/*.java" />
				<exclude name="**/*.html" />
				<exclude name="**/*.psd" />
			</fileset>
		</copy>
	</target>

	<target name="clean" description="clean up">
		<delete dir="${build}" />
	</target>

	<target name="jar-core" depends="compile">
		<jar destfile="${build.jars}/${project.jar.prefix}core.jar" basedir="${build.classes}" includes="org/cagrid/**/*.class images/**" />
	</target>

	<target name="jar" depends="jar-core">
	</target>

	<target name="all" depends="clean,compile,jar">
	</target>

	<target name="run" depends="all">
		<java classname="org.cagrid.installer.CaGridInstallerWizard" fork="true">
			<classpath>
				<fileset dir="${lib.dir}">
					<include name="**/*.jar" />
				</fileset>
				<fileset dir="${build.jars}">
					<include name="**/*.jar" />
				</fileset>
				<fileset dir="${ant.library.dir}">
					<include name="*.jar" />
				</fileset>
			</classpath>
		</java>
	</target>



	<!--
	
	<target name="dist" depends="compile" description="generate the distribution">

		<mkdir dir="${dist}/lib" />
		<jar jarfile="${dist}/lib/${cagrid.master.jar.prefix}systemCheck.jar" basedir="${build}" />
		<copy todir="${dist}/lib">
			<fileset dir="${lib.dir}">
				<include name="**/*.jar" />
			</fileset>
		</copy>
	</target>
	
	<target name="jar-certificate-tasks" depends="compile">
		<jar destfile="${dist}/${project.jar.prefix}certificate_tasks.jar" basedir="${build.classes}" includes="**/customtasks/*.class" />
	</target>
	
	<target name="signjar" depends="generatekey" description="signs the jars">
		<signjar jar="${dist}/lib/ant.jar" alias="installer" storepass="installerpassword" keystore="${dist}/lib/installerKeys" />
		<signjar jar="${dist}/lib/${project.jar.prefix}systemCheck.jar" alias="installer" storepass="installerpassword" keystore="${dist}/lib/installerKeys" />
	</target>

	<target name="generatekey" depends="dist" description="generates keys">
		<genkey alias="installer" storepass="installerpassword" keystore="${dist}/lib/installerKeys" dname="CN=Ant Group, OU=Jakarta Division, O=Apache.org, C=US" />
	</target>

	<target name="copy-common-installer-jars">
		<copy todir="installers/common/installer/template/installclasspath/resources/lib/ext">
			<fileset dir="ext/lib">
				<include name="${cagrid.master.jar.prefix}core.jar" />
			</fileset>
			<fileset dir="${env.GLOBUS_LOCATION}/lib">
				<include name="jce-jdk13-125.jar" />
				<include name="cog-jglobus.jar" />
			</fileset>
		</copy>
		<copy todir="installers/common/installer/template/installclasspath/resources/lib">
			<fileset dir="deployer/lib">
				<include name="xmltask-v1.14.jar" />
			</fileset>
			<fileset dir="${dist}">
				<include name="certificate_tasks.jar" />
			</fileset>
		</copy>
	</target>
	-->



</project>


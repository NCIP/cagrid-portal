<?xml version="1.0"?>
<project name="developers-build-deploy file" basedir=".">
	<!-- ============================================================== -->
	<!-- This file is designed to allow the developer to extend the     -->
	<!-- deploy process without having to edit the build-deploy.xml     -->
	<!-- file. DO NOT REMOVE ANY TARGETS                                -->
	<!-- ============================================================== -->


	<!-- ============================================================== -->
	<!-- Pre Deploy Tomcat                                              -->
	<!-- ============================================================== -->
	<target name="preDeployTomcat">
		<copy todir="${tomcat.dir}/common/endorsed">
			<fileset dir="${ext.endorsed.dir}">
				<include name="*.jar" />
			</fileset>
		</copy>
		<copy todir="${build.lib.dir}">
			<fileset dir="${ext.lib}">
				<include name="*.jar" />
			</fileset>
			<!-- Eracom -->
					    <fileset dir="${lib.dir}/eracom">
							<include name="jce.jar" />
							<include name="jprov.jar" />
					   </fileset>
		</copy>
		
		
		
	</target>


	<!-- ============================================================== -->
	<!-- Pre Deploy Globus                                              -->
	<!-- ============================================================== -->
	<target name="preDeployGlobus">
		<copy todir="${ext.globus.dir}/endorsed">
			<fileset dir="${ext.endorsed.dir}">
				<include name="*.jar" />
			</fileset>
		</copy>
		<copy todir="${build.lib.dir}">
			<fileset dir="${ext.lib}">
				<include name="*.jar" />
			</fileset>
			<!-- Eracom -->
					    <fileset dir="${lib.dir}/eracom">
							<include name="jce.jar" />
							<include name="jprov.jar" />
					   </fileset>
		</copy>
	</target>

</project>
